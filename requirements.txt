# Core
playwright>=1.45.0
pydantic>=2.7.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
click>=8.1.7
rich>=13.7.1

# Resume parsing
pdfplumber>=0.11.0
python-docx>=1.1.2

# LLM (Gemini free tier, via plain REST calls — see src/llm.py)
requests>=2.31.0

# Storage
# sqlite3 is part of the Python standard library, no install needed

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0
